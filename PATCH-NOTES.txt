KAELHAX UI RESTORE — V2 BASELINE + USER SIDEBAR SPACING

Restores the known-good Cloudinary Media Library V2 UI baseline and adds only the user sidebar icon/text spacing adjustment.

Included:
- app/globals.css — V2 UI baseline + user sidebar spacing only
- app/admin/page.js — V2 admin UI / Cloudinary Media Library
- app/api/cloudinary/route.js — V2 Cloudinary route
- lib/cloudinary-client.js — V2 Cloudinary client helper

Not included:
- app/page.js
- firestore.rules
- environment files / secrets

This patch is intended to preserve the existing user storefront code and current Firestore rules while restoring the V2 Admin/Media UI styling and the user sidebar spacing.
